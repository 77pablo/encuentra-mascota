EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_WEB_URL=
EXPO_PUBLIC_SENTRY_DSN=
GOOGLE_MAPS_API_KEY=
# Push real: id del proyecto de EAS. Lo entrega `eas init` (o cópialo del
# dashboard de Expo). Lo consume src/services/pushTokens.ts vía app.config.ts.
EAS_PROJECT_ID=
# NOTA correo/SMTP: NO va ninguna llave de correo aquí. El SMTP (Resend/Brevo)
# se configura en el dashboard de Supabase (Authentication > Emails > SMTP),
# no en la app. Ver SETUP-PUSH-CORREO.md.
